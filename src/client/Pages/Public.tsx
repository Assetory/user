import React from 'react';

const Public : React.FunctionComponent = () : React.ReactElement =>
{
    return (
        <div>
            Public Component
        </div>
    );
};

export default Public;
