import React from 'react';

const Profile : React.FunctionComponent = () : React.ReactElement =>
{
    return (
        <div>
            Profile Component
        </div>
    );
};

export default Profile;
